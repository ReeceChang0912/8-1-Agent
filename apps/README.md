# Apps

This repo is organized as a monorepo:

- `apps/backend`: FastAPI backend
- `apps/web`: Web app
- `apps/miniapp`: WeChat mini program shell

The backend serves the shared API for both clients.
