<view class="page">
  <view class="summary">
    <view class="summary-item">
      <text class="num">{{readyCount}}</text>
      <text class="label">已上线</text>
    </view>
    <view class="summary-item">
      <text class="num">{{plannedCount}}</text>
      <text class="label">规划中</text>
    </view>
  </view>

  <view class="module-list">
    <block wx:for="{{modules}}" wx:key="id">
      <view class="module-card" bindtap="openModule" data-module="{{item}}">
        <view class="head">
          <text class="title">{{item.title}}</text>
          <text class="status {{item.status}}">{{item.status === 'ready' ? '已上线' : '规划中'}}</text>
        </view>
        <view class="desc">{{item.description}}</view>
        <view class="meta">
          <text>{{item.category}}</text>
          <text>{{item.owner}}</text>
        </view>
      </view>
    </block>
  </view>
</view>
